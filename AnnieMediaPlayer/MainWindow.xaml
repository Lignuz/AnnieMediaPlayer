<Window x:Class="AnnieMediaPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AnnieMediaPlayer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <!-- 비디오 표시 영역 -->
        <Image Name="VideoImage" Stretch="Uniform" Grid.Row="0" />

        <!-- 하단 정보 -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Left">
            <TextBlock Text="Frame: " VerticalAlignment="Center"/>
            <TextBlock Name="FrameLabel" Margin="5,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 파일 열기 버튼 -->
        <Button Content="Open Video" HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="10" Click="OpenVideo_Click"/>
    </Grid>
</Window>
